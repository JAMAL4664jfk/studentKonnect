{
  "name": "studentkonnect-blockchain",
  "version": "1.0.0",

  "description": "Blockchain infrastructure for Student Konnect",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.ts",
    "deploy:local": "hardhat run scripts/deploy.ts --network localhost",
    "node": "hardhat node",
    "test": "hardhat test"
  }
}
